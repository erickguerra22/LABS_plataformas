<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".HomeFragment"
    android:background="@color/light_black"
    >

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/txt_fragmentHome_salute"
        android:textSize="@dimen/Title_1"
        android:textColor="@color/white_2"
        tools:text="Good morning,"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_blank_fragment"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/txt_fragmentHome_name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:textAlignment="center"
        app:layout_constraintVertical_chainStyle="packed"
        />
    <TextView
        android:id="@+id/txt_fragmentHome_name"
        android:layout_width="0dp"
        android:textColor="@color/gray_1"
        android:text="@string/name"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/txt_fragmentHome_salute"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@id/txt_fragmentHome_salute"
        app:layout_constraintEnd_toEndOf="parent"
        android:textSize="@dimen/subtitle_1"
        />

</androidx.constraintlayout.widget.ConstraintLayout>